# My neovim config 

## Commands to install this nvim config 
```
# create a backup of current nvim config
mv ~/.config/nvim/ nvim_save
# clone the repo
cd ~/.config/
git clone https://github.com/Goodmorningpeople/nvim
```
